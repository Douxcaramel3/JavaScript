/**
 * Las funciones anónimas son comunes en JavaScript. Donde se nombre la function(), pero no tienen nombre. Se les puede asignar una variable si se invoca a través de esta.
 */

let hola=function(){ 
    alert("Hola Mundo");
}

//otros ejemplos
const cuadrado=function(numero){ //es una constante
    return numero*numero;
}
console.log(cuadrado(5));

